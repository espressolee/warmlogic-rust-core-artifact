[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wl-governance-mcp"
version = "1.0.0"
description = "WarmLogic Governance MCP Server"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "mcp>=1.0.0",
    "pyyaml>=6.0",
]

[project.scripts]
wl-governance-mcp = "server:main"

[tool.hatch.build.targets.wheel]
packages = ["."]
