# WarmLogic Core Dependencies
# Synced with pyproject.toml [project.dependencies]
# Versions pinned for reproducibility (2026-02-14)

# Core Framework
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
typer==0.21.1

# Asynchronous Runtime
anyio==4.12.1

# HTTP Client/Server
requests==2.32.5
httpx==0.28.1
urllib3==2.6.3

# Observability
prometheus-client==0.24.1
python-json-logger==4.0.0

# Security
cryptography==46.0.5

# Utilities
python-dotenv==1.2.1

# Note: The 'warm_logic_rs' Rust Core is required for production.
# Build via: cd rust_core && maturin develop --release --features 'python,std,persistence'
